// JavaScript for toggling the navigation menu visibility
function toggleMenu() {
    const navList = document.querySelector(".nav-list");
    // Toggle the 'active' class on the navigation menu
    navList.classList.toggle("active");
}
